# Pre-edit audit findings

Audit baseline: local commit `cad2f352c9da2fe7b1d3b96f54341241fb0d16b3`
on branch `main`. The worktree was clean before this file was added.

| ID | Severity | Local component | Finding | Evidence | Planned disposition |
| --- | --- | --- | --- | --- | --- |
| F-01 | Critical | `src/benchmarks/vidhalluc/evaluator.py` | BQA is scored per clip record, but the official evaluator scores a question correct only when every clip answer in that question is correct. | VidHalluc `e753864`, `eval/evaluation/eval_bqa.py:25-54`. | Group records by source question and count missing/unparseable clips as incorrect. |
| F-02 | Critical | `src/benchmarks/vidhalluc/evaluator.py` | STH is reported as binary accuracy. The official score is `0.6 * ((MCC+1)/2)^2 + 0.4 * description_score`, with SimCSE-based location matching. | VidHalluc `e753864`, `eval/evaluation/eval_sth.py:84-144`; paper Sec. 4/Table 1. | Stop publishing binary accuracy as official STH; report components and `N/A` until description judging is available. |
| F-03 | High | `src/benchmarks/vidhalluc/loader.py` | Missing videos and malformed labels are silently skipped, changing denominators. | Local loader lines 141-146, 166-171, 186-188, 211-217. | Add validation accounting and preserve unresolved samples as explicit failures where an annotation is otherwise scoreable. |
| F-04 | High | `src/benchmarks/videohallucer/evaluator.py` | Incomplete pairs and duplicate records are excluded from the denominator. | Local evaluator lines 15-21; official VideoHallucer `8b785d1`, `evaluations/evaluation_utils.py:81-113`. | Deduplicate by sample identity and count incomplete pairs as incorrect. |
| F-05 | High | `src/benchmarks/eventhallusion/evaluator.py` | Binary denominator is currently correct, but `n_valid` can be mistaken for the metric denominator and no fixed split inventory is emitted. | EventHallusion `aa544c2`, `eval_binary.py:15-33` counts every question, including parser failures as wrong. | Keep all-record denominator and expose correct/parser/missing counts per split and overall. |
| F-06 | Critical | Qwen/LLaVA DINO-HEAL adapters | Saliency is token-norm pooled to one scalar per frame, not final-layer head-averaged CLS-to-patch attention aligned to the visual patch grid. | DINO-HEAL paper Eq. 1-5; VidHalluc `e753864`, `DINO-HEAL/Video-LLaVA/clip_encoder.py:109-182`. | Mark adapters partial and unsupported for research results; retain no silent CLIP-only fallback. |
| F-07 | Critical | Qwen SEASON adapter | Temporal homogenization is applied once to pixel input and frame attention bucketizes an unrelated sequence axis. It is not layer-wise vision homogenization nor exact visual-token attention mapping. | SEASON paper Eq. 2 and Eq. 4; local `qwen25_vl.py` branch transform and `_summarize_frame_attention`. | Mark SEASON unsupported. Do not publish its outputs. |
| F-08 | High | `src/methods/season/positive_features.py` | The so-called positive branch is not part of the SEASON paper. | SEASON arXiv:2512.04643 Sec. 3 uses original, Gaussian spatial negative and temporal negative only. | Document as an unrelated experimental extension; do not claim SEASON equivalence. |
| F-09 | High | TCD provenance | EventHallusion publishes inference/evaluation templates but no confirmed complete token-level TCD implementation. Local TCD is a paper-based reimplementation. | EventHallusion `aa544c2` tree; TCD paper Eq. 1-6. | Attribute as reimplementation and retain formula/subset tests. |
| F-10 | Critical | `src/models/compatibility.py` | Several combinations are `True` while their own notes say validation remains pending; the matrix also contradicts `tests/test_compatibility.py`. | Local compatibility lines 5-14 and test lines 6-9. | Mark unverified/partial pairs not runnable until artifact-backed GPU validation exists. |
| F-11 | High | `scripts/aggregate_results.py` | Missing values are blank, values remain 0-1, incomplete task averages can be shown, and the requested four final artifacts/header are absent. | Local aggregator lines 27-145. | Emit `N/A`, percentages, fixed rows, training-free column, strict AVG prerequisites, and CSV/MD/TeX/JSON. |
| F-12 | Medium | `external/` provenance | Locked SHAs are correct and checked snapshots match upstream, but provenance/license status is undocumented. EventHallusion and VidHalluc have no top-level license file at the locked SHAs. | Sparse clones at VideoHallucer `8b785d1`, EventHallusion `aa544c2`, VidHalluc `e753864`; zero relevant diffs. | Add provenance report; do not imply redistribution permission for unlicensed upstreams. |
| F-13 | Critical | result aggregation | `write_metrics_bundle` and the standalone evaluator can combine JSONL from different experiment names, contaminating current metrics with smoke/old runs. | Local runner globbed all `results/raw/*.jsonl`; prior repeated runs share model/method/task identities. | Scope runner aggregation to the current experiment prefix and reject ambiguous multi-experiment directories unless `--experiment` is supplied. |
