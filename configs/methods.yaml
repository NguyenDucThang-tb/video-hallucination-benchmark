methods:
  base:
    batch_size: 1
  tcd:
    batch_size: 1
    alpha: 0.5
    beta: 0.5
    # TCD papers use r for the number of frames retained in the contrastive video.
    negative_frame_count: 4
    threshold_space: raw_logits
    all_masked_behavior: original_argmax
  dino_heal:
    batch_size: 1
    dino_checkpoint: /scratch/jp09/dd9648/huggingface/dinov2-large
    dino_device: cpu
    visual_weight: 0.3
    saliency_weight: 0.7
    require_dino: true
  season:
    batch_size: 1
    alpha: 1.0
    homogenization_beta: 0.33
    spatial_noise_std: 0.1
    attention_layers: [20, 21, 22, 23]
    expected_frame_count: 8
    epsilon: 1.0e-8
  positive_feature:
    batch_size: 1
    dino_checkpoint: /scratch/jp09/dd9648/huggingface/dinov2-large
    dino_device: cpu
    alpha: 0.4
    alpha_s: 0.4
    beta: 0.4
    epsilon: 1.0e-6
    foreground_threshold: 0.5
    foreground_morph_kernel: 0
    foreground_return_soft: true
    foreground_pair_fusion: mean
    foreground_pool_avg_weight: 1.0
    require_dino: true
